# Protocol Contracts

Create this only when the repo has protocol/message/file/CLI/SDK contracts that future changes must preserve.

## Contract type

Unknown / To verify.

Examples:
- HTTP/RPC/GraphQL API
- WebSocket
- event/message queue
- CLI command
- SDK/library interface
- file format
- database schema boundary
- external integration

## Entry points

Unknown / To verify.

## Message/request format

Unknown / To verify.

## Response/event/output format

Unknown / To verify.

## Compatibility rules

- Do not change contract shape unless explicitly asked.
- If changed, update tests and this document.
- Mark uncertain behavior as `Unknown` or `To verify`.
