import { r as Extension } from "./dist-BALbLdl2.js";
import "./@tiptap_extension-text-style.js";
//#region node_modules/@tiptap/extension-color/dist/index.js
/**
* This extension allows you to color your text.
* @see https://tiptap.dev/api/extensions/color
*/
var Color = Extension.create({
	name: "color",
	addOptions() {
		return { types: ["textStyle"] };
	},
	addGlobalAttributes() {
		return [{
			types: this.options.types,
			attributes: { color: {
				default: null,
				parseHTML: (element) => {
					var _a;
					return (_a = element.style.color) === null || _a === void 0 ? void 0 : _a.replace(/['"]+/g, "");
				},
				renderHTML: (attributes) => {
					if (!attributes.color) return {};
					return { style: `color: ${attributes.color}` };
				}
			} }
		}];
	},
	addCommands() {
		return {
			setColor: (color) => ({ chain }) => {
				return chain().setMark("textStyle", { color }).run();
			},
			unsetColor: () => ({ chain }) => {
				return chain().setMark("textStyle", { color: null }).removeEmptyTextStyle().run();
			}
		};
	}
});
//#endregion
export { Color, Color as default };

//# sourceMappingURL=@tiptap_extension-color.js.map